For Running The code please follow the following steps ------

1. First install jupyter notebook in any python environment or,
   Install Anaconda for acceessing the jupyter notebook in your PC.

2. Before runnin the .ipynb file in the jupyter notebook you need to
   install the following libraries into the jupyter notebook cell by using
   following commands - 

   !pip install pygame
   !pip install JSAnimation
   !pip install numpy
   !pip install pandas

you can just copy the following commands and run to the initial cell of the
jupyter notebook.

And you are good to go .........